#include<bits/stdc++.h>
using namespace std;
#define fr(name) freopen(name, "r", stdin);
#define fw(name) freopen(name, "w", stdout);

void solve() {
    int n;
    cin >> n;
}
int main() {
    solve();
    return 0;
}
